
#include <vector>
#include <queue>

queue<circuit> circuits;

vector <vector <bool>> rec_function (vector < vector <bool>> output) {

	// check if output is equal to desired result
	// PROBLEM: kill node based on criteria

	// PROBLEM: add gates to new circuit 
	// store the circuit in queue

	// rec_fuction (ciruits.front().get_output())

}