share/scripts/git-hooks
===============

hooks for git. If you want to use them put it in $PROJECT_ROOT/.git/hooks

- pre-push := a git hook that runs each time you attempt to push and only lets you push if build succeeds.
